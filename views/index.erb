<article>
  <ul>
    <% @memo["memos"].each do |memo| %>
      <% next unless memo["id"] %>
      <li>
        <a href= "/memos/<%= memo["id"] %>" ><%= memo["title"] %></a>
      </li>
    <% end %>
  </ul>
</article>
